program
	i:integer;
	r:real;
	j:1..13;
	b:boolean;
	ai:Array [1..5] of integer;
	ar:Array [1..5] of real;
	aj:Array [1..5] of 1..13;
begin
	b := not r = r;
	b := not r = i;
	b := not r = j;
	b := not r = ai[1];
	b := not r = aj[1];
	b := not r = ar[1];
	b := not r < r;
	b := not r < i;
	b := not r < j;
	b := not r < ai[1];
	b := not r < aj[1];
	b := not r < ar[1];
	b := not r > r;
	b := not r > i;
	b := not r > j;
	b := not r > ai[1];
	b := not r > aj[1];
	b := not r > ar[1];
	b := not r /= r;
	b := not r /= i;
	b := not r /= j;
	b := not r /= ai[1];
	b := not r /= aj[1];
	b := not r /= ar[1];
	b := not r <= r;
	b := not r <= i;
	b := not r <= j;
	b := not r <= ai[1];
	b := not r <= aj[1];
	b := not r <= ar[1];
	b := not r >= r;
	b := not r >= i;
	b := not r >= j;
	b := not r >= ai[1];
	b := not r >= aj[1];
	b := not r >= ar[1];
end.